<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.franz.wallet">

    <meta-data
        android:name="DATABASE"
        android:value="sugar_exampled.db" />
    <meta-data
        android:name="VERSION"
        android:value="4" />
    <meta-data
        android:name="QUERY_LOG"
        android:value="true" />
    <meta-data
        android:name="DOMAIN_PACKAGE_NAME"
        android:value="com.example" />
    `
    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@drawable/wallet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.Registro" />
        <activity
            android:name=".Activities.InicioActivity"
            android:label="@string/title_activity_inicio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.InsertarMovimiento" />
        <activity android:name=".Activities.Gestion" />
        <activity
            android:name=".Activities.CategoriaActivity"
            android:label="@string/title_activity_categoria"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.List_Movimientos_Activity" />
        <activity
            android:name=".Activities.DetalleGestionActivity"
            android:label="@string/title_activity_detalle_gestion"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.abm_categoria_activity"
            android:label="@string/title_activity_abm_categoria_activity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>